# salih.dev

A text-first personal website built with Astro and TypeScript.

## Commands

```sh
npm install
npm run dev
npm run check
npm run build
npm start
```

The production server listens on port `4321` by default. Set `HOST` and `PORT`
when a hosting environment requires different values.
